//#region src/utils/string.ts
const toSnakeCase = (str) => str.split(" ").map((word) => word.toLowerCase()).join("_");
const isObject = (value) => value != null && value === Object(value);
//#endregion
export { isObject, toSnakeCase };
