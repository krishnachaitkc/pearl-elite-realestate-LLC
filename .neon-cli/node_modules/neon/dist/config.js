import { isCi } from "./env.js";
import { a as isOwnedCredentialPath, i as isInsideConfigDir, n as credentialsPath, r as defaultDir, t as CREDENTIALS_FILE } from "./_chunks/paths-DMq0Lt7a.js";
import { existsSync, mkdirSync } from "node:fs";
//#region src/config.ts
const ensureConfigDir = ({ "config-dir": configDirArg, "force-auth": forceAuth }) => {
	if (!existsSync(configDirArg) && (!isCi() || forceAuth)) mkdirSync(configDirArg, { recursive: true });
};
//#endregion
export { CREDENTIALS_FILE, credentialsPath, defaultDir, ensureConfigDir, isInsideConfigDir, isOwnedCredentialPath };
