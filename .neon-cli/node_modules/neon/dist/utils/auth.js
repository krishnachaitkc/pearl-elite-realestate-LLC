//#region src/utils/auth.ts
const extendTokenSet = (tokenSet) => {
	const exp = /* @__PURE__ */ new Date();
	exp.setSeconds(exp.getSeconds() + (tokenSet.expires_in ?? 0));
	return {
		...tokenSet,
		expires_at: exp.getTime()
	};
};
//#endregion
export { extendTokenSet };
