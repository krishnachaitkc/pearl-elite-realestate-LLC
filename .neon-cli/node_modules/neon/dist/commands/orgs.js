import { t as __exportAll } from "../_chunks/rolldown-runtime-8H4AJuhK.js";
import { writer } from "../writer.js";
//#region src/commands/orgs.ts
var orgs_exports = /* @__PURE__ */ __exportAll({
	aliases: () => aliases,
	builder: () => builder,
	command: () => command,
	describe: () => describe,
	handler: () => handler
});
const ORG_FIELDS = ["id", "name"];
const command = "orgs";
const describe = "Manage organizations";
const aliases = ["org"];
const builder = (argv) => {
	return argv.usage("$0 orgs <sub-command> [options]").command("list", "List organizations", (yargs) => yargs, async (args) => {
		await list(args);
	});
};
const handler = (args) => {
	return args;
};
const list = async (props) => {
	const out = writer(props);
	const { data: { organizations } } = await props.apiClient.getCurrentUserOrganizations();
	out.write(organizations, {
		fields: ORG_FIELDS,
		title: "Organizations",
		emptyMessage: "You are not a member of any organization."
	});
	out.end();
};
//#endregion
export { aliases, builder, command, describe, handler, orgs_exports as t };
