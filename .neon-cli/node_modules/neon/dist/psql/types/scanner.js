//#region src/psql/types/scanner.ts
/**
* Returns true if the scanner status represents an unterminated statement
* waiting for more input. Useful for callers that only care whether to draw
* PROMPT2 vs PROMPT1 and don't care about the specific reason.
*/
const isContinueStatus = (s) => s === "continue" || s === "continue-quote" || s === "continue-dquote" || s === "continue-dollar" || s === "paren" || s === "comment";
const initialScanState = () => ({
	promptStatus: "ready",
	parenDepth: 0,
	dollarTag: null,
	inLineComment: false,
	inBlockComment: 0,
	inSingleQuote: false,
	inDoubleQuote: false,
	inEscapeString: false,
	beginDepth: 0,
	identifierLetters: [
		"",
		"",
		"",
		""
	],
	identifierCount: 0
});
//#endregion
export { initialScanState, isContinueStatus };
