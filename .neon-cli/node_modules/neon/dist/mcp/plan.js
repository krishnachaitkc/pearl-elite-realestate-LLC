import { tryResolveAddMcpAgentId } from "./agents.js";
import { detectMcpAgents, mcpInstallableAgents } from "./targets.js";
import { collectYesAgents, noDetectedAgentsMessage } from "../init/plan.js";
import { agentChoicesFrom, pickAgentsInteractively, resolveAgentSelection } from "../utils/agent_picker.js";
import { detectAgent } from "../init/detect_host.js";
import { pickMcpAuth, pickMcpProjectPin, pickMcpScope } from "./wizard.js";
//#region src/mcp/plan.ts
async function resolveMcpPlan(options) {
	const prompt = options.interactive && !options.yes;
	const scope = options.project ? "project" : prompt ? await (options.pickScope ?? pickMcpScope)() : "global";
	const available = mcpInstallableAgents(scope);
	const availableSet = new Set(available);
	const scoped = await detectMcpAgents({
		scope,
		cwd: options.cwd
	});
	const detected = options.yes && options.agents.length === 0 ? await collectYesAgents({
		detected: () => scoped,
		detectAgent: options.detectAgent ?? detectAgent,
		acceptHost: (id) => availableSet.has(id)
	}) : scoped;
	const agents = await resolveAgentSelection({
		specified: options.agents,
		choices: agentChoicesFrom(available, detected),
		detected,
		message: "Which coding agents should get the Neon MCP server? (space to toggle, enter to confirm)",
		nonInteractiveMessage: noDetectedAgentsMessage({
			scope,
			supported: available,
			fix: options.yes ? "run-without-yes" : "pass-yes",
			nameAgent: true
		}),
		resolveSpecified: (raw) => {
			const id = tryResolveAddMcpAgentId(raw);
			if (!id) throw new Error(`Unknown agent: "${raw}". Supported agents: ${available.join(", ")}`);
			return id;
		},
		pick: prompt ? options.pickAgents ?? pickAgentsInteractively : void 0,
		interactive: prompt
	});
	const auth = options.oauth ? "oauth" : prompt ? await (options.pickAuth ?? pickMcpAuth)() : "api-key";
	let urlProjectId = options.projectId;
	if (urlProjectId === void 0 && prompt && scope === "project" && options.linkedProjectId) {
		if (await (options.pickProjectPin ?? pickMcpProjectPin)(options.linkedProjectId, auth === "api-key")) urlProjectId = options.linkedProjectId;
	}
	return {
		scope,
		agents,
		auth,
		readOnly: options.readOnly,
		urlProjectId,
		categories: options.categories
	};
}
//#endregion
export { resolveMcpPlan };
