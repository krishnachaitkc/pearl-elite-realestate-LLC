import { randomUUID } from "node:crypto";
import { createServer } from "node:http";
//#region src/test_utils/rotating_oauth_server.ts
const startRotatingOauthServer = async (options = {}) => {
	const lifetime = options.accessTokenLifetimeSeconds ?? 3600;
	const liveRefreshTokens = /* @__PURE__ */ new Set();
	let refreshAttempts = 0;
	let rotations = 0;
	let unreachable = false;
	const issue = ({ expiresIn = lifetime } = {}) => {
		const refresh = `refresh-${randomUUID()}`;
		liveRefreshTokens.add(refresh);
		return {
			access_token: `access-${randomUUID()}`,
			refresh_token: refresh,
			expires_in: expiresIn,
			token_type: "bearer"
		};
	};
	const server = createServer((req, res) => {
		const url = new URL(req.url ?? "/", `http://${req.headers.host}`);
		if (url.pathname === "/.well-known/openid-configuration") return json(res, 200, {
			issuer: baseUrl(),
			token_endpoint: `${baseUrl()}/oauth2/token`,
			authorization_endpoint: `${baseUrl()}/oauth2/authorize`,
			revocation_endpoint: `${baseUrl()}/oauth2/revoke`,
			response_types_supported: ["code"],
			grant_types_supported: ["authorization_code", "refresh_token"],
			token_endpoint_auth_methods_supported: ["none"],
			scopes_supported: [
				"openid",
				"offline",
				"offline_access"
			]
		});
		if (url.pathname === "/oauth2/token" && req.method === "POST") return readBody(req, (body) => {
			const form = new URLSearchParams(body);
			if (form.get("grant_type") !== "refresh_token") return json(res, 400, { error: "unsupported_grant_type" });
			refreshAttempts += 1;
			if (unreachable) {
				res.destroy();
				return;
			}
			const presented = form.get("refresh_token") ?? "";
			if (!liveRefreshTokens.delete(presented)) return json(res, 400, {
				error: "invalid_grant",
				error_description: "The provided authorization grant or refresh token is invalid, expired, or revoked."
			});
			rotations += 1;
			return json(res, 200, {
				...issue(),
				scope: "openid offline offline_access"
			});
		});
		return json(res, 404, { error: "not_found" });
	});
	await new Promise((resolve) => {
		server.listen(0, "127.0.0.1", resolve);
	});
	const baseUrl = () => `http://127.0.0.1:${server.address().port}`;
	return {
		url: baseUrl(),
		refreshAttempts: () => refreshAttempts,
		rotations: () => rotations,
		issue,
		revoke: (refreshToken) => {
			liveRefreshTokens.delete(refreshToken);
		},
		setUnreachable: (value) => {
			unreachable = value;
		},
		stop: () => closeServer(server)
	};
};
const json = (res, status, body) => {
	const payload = JSON.stringify(body);
	res.writeHead(status, {
		"content-type": "application/json;charset=UTF-8",
		"content-length": Buffer.byteLength(payload)
	});
	res.end(payload);
};
const readBody = (req, next) => {
	let body = "";
	req.on("data", (chunk) => {
		body += chunk.toString();
	});
	req.on("end", () => next(body));
};
const closeServer = (server) => new Promise((resolve, reject) => {
	server.close((err) => err ? reject(err) : resolve());
});
//#endregion
export { startRotatingOauthServer };
