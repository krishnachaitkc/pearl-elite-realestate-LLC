import { getAgentDisplayName } from "../mcp/agents.js";
import "../init/agents.js";
import { canPickAgentsInteractively } from "../utils/agent_picker.js";
import { NEON_PLUGIN_NAME } from "./run.js";
import prompts from "prompts";
//#region src/plugins/wizard.ts
const restoreCursorOnAbort = (state) => {
	if (state.aborted) {
		process.stdout.write("\x1B[?25h");
		process.stdout.write("\n");
		process.exit(1);
	}
};
const pluginsInstallSummary = (options) => {
	const rows = [
		["Scope", options.scope === "project" ? "project" : "user"],
		["Agents", options.agents.map(getAgentDisplayName).join(", ")],
		["Plugin", NEON_PLUGIN_NAME]
	];
	const labelWidth = Math.max(...rows.map(([label]) => label.length));
	return rows.map(([label, value]) => `${label.padEnd(labelWidth)}  ${value}`).join("\n");
};
const confirmPluginsInstall = async (options) => {
	if (!canPickAgentsInteractively()) return true;
	process.stdout.write(`\n${pluginsInstallSummary(options)}\n\n`);
	const { ok } = await prompts({
		onState: restoreCursorOnAbort,
		type: "confirm",
		name: "ok",
		message: "Install the Neon plugin into these agents?",
		initial: true
	});
	return ok === true;
};
//#endregion
export { confirmPluginsInstall, pluginsInstallSummary };
