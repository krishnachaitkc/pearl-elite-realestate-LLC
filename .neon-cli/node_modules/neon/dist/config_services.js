//#region src/config_services.ts
const isToggleEnabled = (toggle) => {
	if (toggle === void 0) return false;
	if (typeof toggle === "boolean") return toggle;
	return toggle.enabled !== false;
};
/** Postgres is omitted because every project includes it. */
const declaredNeonServices = (config) => {
	const services = [];
	if (isToggleEnabled(config.auth)) services.push("auth");
	if (isToggleEnabled(config.dataApi)) services.push("data-api");
	if (Object.keys(config.preview?.buckets ?? {}).length > 0) services.push("object-storage");
	if (Object.keys(config.preview?.functions ?? {}).length > 0) services.push("functions");
	if (isToggleEnabled(config.preview?.aiGateway)) services.push("ai-gateway");
	return services;
};
const dataApiSettingsToSnake = (settings) => {
	const out = {};
	if (settings.dbAggregatesEnabled !== void 0) out.db_aggregates_enabled = settings.dbAggregatesEnabled;
	if (settings.dbAnonRole !== void 0) out.db_anon_role = settings.dbAnonRole;
	if (settings.dbExtraSearchPath !== void 0) out.db_extra_search_path = settings.dbExtraSearchPath;
	if (settings.dbMaxRows !== void 0) out.db_max_rows = settings.dbMaxRows;
	if (settings.dbSchemas !== void 0) out.db_schemas = settings.dbSchemas;
	if (settings.jwtRoleClaimKey !== void 0) out.jwt_role_claim_key = settings.jwtRoleClaimKey;
	if (settings.jwtCacheMaxLifetime !== void 0) out.jwt_cache_max_lifetime = settings.jwtCacheMaxLifetime;
	if (settings.openapiMode !== void 0) out.openapi_mode = settings.openapiMode;
	if (settings.serverCorsAllowedOrigins !== void 0) out.server_cors_allowed_origins = settings.serverCorsAllowedOrigins;
	if (settings.serverTimingEnabled !== void 0) out.server_timing_enabled = settings.serverTimingEnabled;
	return out;
};
/** Omitting this body preserves the server heuristic used by `--service data-api`. */
const claimableDataApiCreateBody = (config) => {
	if (!isToggleEnabled(config.dataApi)) return void 0;
	const input = config.dataApi;
	if (typeof input !== "object") return { auth_provider: "neon_auth" };
	const settings = input.settings ? dataApiSettingsToSnake(input.settings) : void 0;
	const withSettings = (body) => settings && Object.keys(settings).length > 0 ? {
		...body,
		settings
	} : body;
	if (input.authProvider === "external") {
		if (input.jwksUrl === void 0 || input.jwksUrl.length === 0) throw new Error("neon.ts dataApi.authProvider \"external\" requires dataApi.jwksUrl.");
		return withSettings({
			auth_provider: "external",
			jwks_url: input.jwksUrl,
			...input.providerName !== void 0 ? { provider_name: input.providerName } : {},
			...input.jwtAudience !== void 0 ? { jwt_audience: input.jwtAudience } : {}
		});
	}
	return withSettings({ auth_provider: "neon_auth" });
};
//#endregion
export { claimableDataApiCreateBody, declaredNeonServices };
