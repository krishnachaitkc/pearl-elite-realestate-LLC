import { t as __exportAll } from "../_chunks/rolldown-runtime-8H4AJuhK.js";
import { fillSingleProject } from "../utils/enrichers.js";
import { applyCmd, applyFlags, envFlag, envPullFlag } from "./config.js";
import { DEPLOY_COMMANDS_EPILOGUE } from "./deploy_help.js";
//#region src/commands/deploy.ts
var deploy_exports = /* @__PURE__ */ __exportAll({
	builder: () => builder,
	command: () => command,
	describe: () => describe,
	handler: () => handler
});
const command = "deploy";
const describe = "Apply a neon.ts policy to a branch (alias for `config apply`)";
const builder = (argv) => argv.usage("$0 deploy [options]").options({
	"project-id": {
		describe: "Project ID",
		type: "string"
	},
	branch: {
		describe: "Branch ID or name",
		type: "string"
	},
	config: {
		describe: "Path to a neon.ts policy (defaults to walking up from cwd)",
		type: "string"
	},
	...envFlag,
	...applyFlags,
	...envPullFlag
}).middleware(fillSingleProject).epilogue(DEPLOY_COMMANDS_EPILOGUE).strict();
const handler = (props) => applyCmd(props);
//#endregion
export { builder, command, describe, handler, deploy_exports as t };
