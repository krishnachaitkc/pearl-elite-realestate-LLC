import { t as __exportAll } from "../_chunks/rolldown-runtime-8H4AJuhK.js";
import { log } from "../log.js";
import { applyContext } from "../context.js";
import { getCliName } from "../utils/cli_name.js";
//#region src/commands/set_context.ts
var set_context_exports = /* @__PURE__ */ __exportAll({
	builder: () => builder,
	command: () => command,
	describe: () => describe,
	handler: () => handler
});
const command = "set-context";
const describe = `Deprecated: use \`${getCliName()} link\`. Set the .neon context (raw write).`;
const builder = (argv) => argv.usage("$0 set-context [options]").options({
	"project-id": {
		describe: "Project ID",
		type: "string"
	},
	"org-id": {
		describe: "Organization ID",
		type: "string"
	},
	"branch-id": {
		describe: "Branch ID",
		type: "string"
	}
});
const handler = (props) => {
	log.warning(`\`${getCliName()} set-context\` is deprecated and will be removed in a future release. Use \`${getCliName()} link\` instead — it verifies inputs and infers the org for you (or \`${getCliName()} link --no-checks\` for the same write-without-checks behavior).`);
	const context = {
		projectId: props.projectId,
		orgId: props.orgId,
		branchId: props.branchId
	};
	applyContext(props.contextFile, context);
};
//#endregion
export { builder, command, describe, handler, set_context_exports as t };
