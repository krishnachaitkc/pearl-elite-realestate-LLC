import { t as __exportAll } from "../_chunks/rolldown-runtime-8H4AJuhK.js";
import { writer } from "../writer.js";
//#region src/commands/user.ts
var user_exports = /* @__PURE__ */ __exportAll({
	builder: () => builder,
	command: () => "me",
	describe: () => describe,
	handler: () => handler
});
const command = "me";
const describe = "Show current user";
const builder = (yargs) => yargs.option("context-file", { hidden: true });
const handler = async (args) => {
	await me(args);
};
const me = async (props) => {
	const { data } = await props.apiClient.getCurrentUserInfo();
	writer(props).end(data, { fields: [
		"login",
		"email",
		"name",
		"projects_limit"
	] });
};
//#endregion
export { builder, command, describe, handler, user_exports as t };
